import { W10ParcelSubStringPipe } from './w10-parcel-sub-string.pipe';

describe('W10ParcelSubStringPipe', () => {
  it('create an instance', () => {
    const pipe = new W10ParcelSubStringPipe();
    expect(pipe).toBeTruthy();
  });
});
